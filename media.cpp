#include <cstdio>  // Incluindo o cabeçalho correto para printf

int main() {
    float nota1, nota2, calc_media;
    nota1 = 5;
    nota2 = 7;
    calc_media = (nota1 + nota2) / 2;
    printf("A media = %f", calc_media);
    
    return 0;  // Adicionando o retorno 0, que é uma boa prática em C++
}
